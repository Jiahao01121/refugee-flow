/*
Create a new file named "config.js" and add your configuration.
The config.js file is in the .gitignore and will not be included in your commits.
*/

module.exports = {
  // API Credentials
  mapboxToken: '',

  // Database Connection
  database: {
    connection: '',
  },
};
